# Sears Home Services Voice AI Agent - Environment Configuration
# Copy this file to .env and fill in your values

# Required: Google Gemini API Key
GOOGLE_API_KEY=your_gemini_api_key_here

# Required: Base URL for webhooks (ngrok URL for development)
APP_BASE_URL=https://your-domain.ngrok-free.app

# Optional: SendGrid for email notifications
SENDGRID_API_KEY=SG.your_sendgrid_api_key
SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# ============================================================================
# Database Configuration (MySQL)
# ============================================================================
# For LOCAL development (running app outside Docker):
# - Set DB_HOST=localhost to connect to your local MySQL server
# - Make sure MySQL is running on your machine
#
# For DOCKER deployment (docker compose up):
# - DB_HOST will be automatically set to 'db' (the service name)
# - No need to change these values, they're overridden in docker-compose.yml
# ============================================================================

# MySQL Connection (for local development)
DB_HOST=localhost
DB_PORT=3306
DB_USER=voice_ai_user
DB_PASSWORD=voiceaipassword
DB_NAME=voice_ai

# Optional: Root password for Docker MySQL container
DB_ROOT_PASSWORD=rootpassword

# Alternative: Use explicit DATABASE_URL (overrides individual DB_* variables)
# DATABASE_URL=mysql+pymysql://voice_ai_user:voiceaipassword@localhost:3306/voice_ai
