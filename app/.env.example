# ============================================================================
# Sears Home Services Voice AI Agent - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values:
#   cp app/.env.example app/.env
# ============================================================================

# ============================================================================
# REQUIRED CREDENTIALS
# ============================================================================

# ngrok Authentication Token (get free at https://ngrok.com)
# Dashboard: https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN=your_ngrok_auth_token_here

# Twilio Credentials (from https://console.twilio.com)
# Account SID and Auth Token: Console → Account Info
# Phone Number SID: Console → Phone Numbers → Click your number → Phone Number SID
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_PHONE_NUMBER_SID=PNxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Google Gemini API Key (from https://aistudio.google.com/app/apikey)
GOOGLE_API_KEY=your_gemini_api_key_here

# ============================================================================
# OPTIONAL CREDENTIALS
# ============================================================================

# SendGrid for email notifications (optional - falls back to console logging)
# Get API key at: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your_sendgrid_api_key
SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# App Base URL (auto-configured by ngrok in Docker, can override for production)
APP_BASE_URL=http://localhost:8000

# ============================================================================
# DATABASE CONFIGURATION (MySQL)
# ============================================================================
# For DOCKER deployment (docker-compose up):
# - These values are automatically overridden by docker-compose.yml
# - No changes needed!
#
# For LOCAL development (without Docker):
# - Set DB_HOST=localhost
# - Make sure MySQL is running on your machine
# ============================================================================

DB_HOST=localhost
DB_PORT=3306
DB_USER=voice_ai_user
DB_PASSWORD=voiceaipassword
DB_NAME=voice_ai
DB_ROOT_PASSWORD=rootpassword
